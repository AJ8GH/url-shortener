spring:
  output:
    ansi:
      enabled: ALWAYS

management:
  endpoints:
    web:
      exposure:
        include: "*"

server:
  servlet:
    context-path: "/api"

logging:
  level:
    org.springframework: INFO
